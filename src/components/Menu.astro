---
import { ROUTES } from "../constants/routes";

const currentPath = Astro.url.pathname;
---

<nav>
  <ul class="grid grid-cols-2 gap-x-8 gap-y-2">
    {
      Object.values(ROUTES).map((route) => (
        <li>
          <a class="relative inline-block group font-medium" href={route.path}>
            <span>{route.label}</span>
            <span
              class:list={[
                "absolute left-0 -bottom-0.5 h-[1px] w-full bg-gruvbox-fg2 transition-transform duration-300 origin-left",
                route.path === currentPath
                  ? "scale-x-100"
                  : "scale-x-0 group-hover:scale-x-100",
              ]}
            />
          </a>
        </li>
      ))
    }
  </ul>
</nav>
